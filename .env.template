# Docker image and container names
IMAGE_NAME=smolvla-finetune:latest
CONTAINER_NAME=smolvla_dev

# Optional: tune output folder name for each run
OUTPUT_NAME=dynamic_lora_pruning_snapflow

# Host paths (relative to project root by default)
DATASET_ROOT=./data/datasets
CHECKPOINT_ROOT=./data/checkpoints
OUTPUT_ROOT_HOST=./outputs
HF_CACHE_ROOT=./data/hf_cache

# Optional tokens / runtime flags
HF_TOKEN=
WANDB_MODE=offline
